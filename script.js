/* ==========================================================================
   Masaya USA Pinktiger - Interactive Enhancements & Multi-language Support
   ========================================================================== */

// 1. Multilingual Translation Database
window.translations = {
  en: {
    "nav-home": "Home",
    "nav-about": "About Masaya",
    "nav-schedule": "Upcoming Schedule",
    "nav-mascot": "Our Mascot",
    "nav-goods": "Digital Merch",
    "nav-support": "Support & Channels",
    "hero-title": "Masaya USA <span style=\"white-space: nowrap;\">Pinktiger</span> <img src=\"./assets/pinktiger.png?v=4\" alt=\"Pinktiger Mascot\" class=\"custom-hero-mascot emoji-bounce\"><span class=\"emoji-pulse\">💖</span>",
    "hero-subtitle": "Experience the voice, vibes, and humanity of Masaya.",
    "hero-tagline": "The US fanbase dedicated to supporting Japanese vocalist Masaya Horikawa's global journey.",
    "hero-btn-join": "Join the Pride 🎀",
    "hero-btn-learn": "Learn More 🐯",
    "video-caption": "<span class=\"label\">Featured Performance:</span> Masaya singing \"첫눈처럼 너에게 가겠다 (I Will Go to You Like the First Snow)\" with English lyrics ❄️🎶",
    "about-section-title": "Masaya's Journey 🐯✨",
    "about-bio-h3": "The Voice that Crosses Borders",
    "about-bio-p1": "<strong>Masaya Horikawa (堀川雅也)</strong> is a powerhouse vocalist from Japan whose emotional range and brilliant stage presence have captured hearts across Asia. Known for his flawless pitch, soaring high notes, and passionate deliveries, he brings a deep sense of vulnerability to every song he sings.",
    "about-bio-p2": "His participation in high-profile singing programs like Japan's <em>THE Karaoke ☆ Battle</em> and the cross-border television sensation <em>Han-Il King of Song / Korea-Japan Top Ten Show</em> established him as a premier vocalist. Masaya performs not just with technique, but with his whole heart, sharing his raw humanity through every lyric.",
    "about-quote": "Singing is the bridge that connects different languages, cultures, and souls. Let's walk this musical path together!",
    "about-stats-h3": "Why We Love Masaya",
    "stat-h4-1": "Vocal Versatility",
    "stat-p-1": "From power ballads to emotional J-Pop covers, his resonance is unmatched.",
    "stat-h4-2": "Warm Personality",
    "stat-p-2": "Always humble, connected, and deeply appreciative of his global fanbase.",
    "stat-h4-3": "Fierce Passion",
    "stat-p-3": "Like a tiger, his determination on stage commands attention and inspires us.",
    "stat-h4-4": "Share Live Updates",
    "stat-p-4": "Click the feed on the left to check out Masaya's latest news and share it with other fans!",
    "gallery-title": "Fandom Gallery 🎀",
    "mascot-title": "Meet Pinktiger! 🎀🐯",
    "mascot-desc": "Created as the mascot of the US Fanbase, **Pinktiger** represents the energy, cute kawaii aesthetic, and bold presence of Masaya's American fanbase.",
    "mascot-label-name": "Name:",
    "mascot-val-name": "Pinktiger",
    "mascot-label-skill": "Special Skill:",
    "mascot-val-skill": "Bouncing to Masaya's high notes 🎶",
    "mascot-label-acc": "Favorite Accessory:",
    "mascot-val-acc": "Polka dot pink bow 🎀",
    "mascot-label-goal": "Goal:",
    "mascot-val-goal": "Spreading love for Masaya across the globe! 🌎💖",
    "mascot-bubble": "\"Roar! Let's cheer for Masaya together! 🐯🎀\"",
    "channels-title": "Support Masaya",
    "channels-subtitle": "🐯 This is an unofficial, fan-made website created out of pure love to support Masaya. (이곳은 순수한 팬심을 함께 나누는 비공식 팬 사이트입니다. / ここはマサヤを応援する純粋なファン心から作られた非公式ファンサイトです。)",
    "btn-yt-main": "Masaya's YouTube",
    "btn-yt-sub": "Subscribe for Cover & Vlogs",
    "btn-ig-main": "Masaya's Instagram",
    "btn-ig-sub": "Follow daily life & updates",
    "btn-sp-main": "Spotify Artist Page",
    "btn-sp-sub": "Stream original music",
    "btn-tk-main": "TikTok Channel",
    "btn-tk-sub": "Watch short music clips",
    "fan-actions-title": "How to Help as a Fan:",
    "fan-action-p-1": "Share video clips on TikTok & Shorts",
    "fan-action-p-2": "Leave encouraging comments in EN/JA/KO",
    "fan-action-p-3": "Add Masaya's covers to your playlists",
    "fan-action-p-4": "Join the USA Pinktiger discussions",
    "footer-desc": "Creating a global home for US-based fans of Japanese vocal sensation Masaya Horikawa.",
    "footer-copyright": "© 2026 Masaya USA Pinktiger - Unofficial Fanbase built with love. 💖",
    "footer-disclaimer": "🐯 Disclaimer: This site is a non-profit fan page voluntarily operated by MasayaUSAPinktiger fans. It is not officially affiliated with Masaya (Masaya Horikawa) or his agency. The copyrights of content such as artist photos and videos used on the site belong to the original copyright holders.",
    "disclaimer-text": "🐯 Disclaimer: This site is a non-profit fan page voluntarily operated by MasayaUSAPinktiger fans. It is not officially affiliated with Masaya (Masaya Horikawa) or his agency. The copyrights of content such as artist photos and videos used on the site belong to the original copyright holders.",
    "fanletters-desc": "Leave a lovely message for Masaya! No login required. Just positive vibes and pink tiger energy. 🐯💖",
    "fanletters-nickname": "<i class=\"fa-solid fa-user\"></i> Nickname",
    "fanletters-country": "<i class=\"fa-solid fa-earth-americas\"></i> Country",
    "fanletters-message": "<i class=\"fa-solid fa-envelope-open-text\"></i> Message",
    "fanletters-nickname-ph": "Your cute nickname",
    "fanletters-country-ph": "Where are you from?",
    "fanletters-message-ph": "Write a short letter to Masaya...",
    "fanletters-send": "<i class=\"fa-solid fa-paper-plane\"></i> Send Letter",
    "fanletters-empty": "Be the first to leave a message! 💌",
    "recent-letters-title": "Recent Letters from Pinktigers 💌",
    "community-btn": "Fan Letters from Afar",
    "map-title": "Masaya's Global Footprint 🗺️",
    "map-subtitle": "Discover fans around the world!",
    "map-modal-title": "Leave a Marker 🐾",
    "map-modal-msg-ph": "Write a cheer message!",
    "map-modal-file-label": "Upload a Photo:",
    "map-modal-submit": "Submit",
    "map-modal-cancel": "Cancel",
    "lbl-location": "Location",
    "lbl-time": "Time",
    "clickMascotHint": "Click the Pinktiger mascot to find Masaya's hidden photos! 🐯✨",
    "ig-updates-title": "Masaya's Latest Updates 🐯✨",
    "teaser_text": "Our Pinktiger is working hard to bring you a fun Masaya footprint tour game! Stay tuned! Also, look forward to our ranking system where the top scorers will be featured on the leaderboard!",
    "nav-tiger-crush": "Tiger Crush",
    "tiger-crush-title": "Tiger Crush 🐯 🐾<span style=\"color: pink;\">🐾</span>",
    "stamp-tour-desc": "Follow Masaya's footprints! Answer quizzes to collect stamps and unlock the next destination!",
    "tiger-crush-subtitle": "Enjoy your complimentary game for Masaya's fandom!",
    "schedule-title": "Masaya's Schedule 📅",
    "sch-title-1": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-1": "Nagoya @JAMMIN'",
    "sch-title-2": "\"Kagero\" Digital Single Release",
    "sch-loc-2": "Online",
    "sch-title-3": "Masaya Concert \"Cheers to the Masterpieces of Life!\"",
    "sch-loc-3": "@YCC Prefectural Cultural Hall",
    "sch-title-4": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-4": "Tokyo @SHIBUYA DIVE",
    "goods-store-title": "Masaya Free Goods Store",
    "goods-store-subtitle": "Download your favorite goods!",
    "download-btn": "Download",
    "goods-title-1": "Nail Art Edition",
    "goods-title-2": "Yukata Edition",
    "goods-title-3": "Concert Outfit Edition",
    "goods-title-4": "Insta Live Edition",
    "goods-title-5": "Luxury Perfume Ambassador Edition",
    "footer-disclaimer": "🐯 Disclaimer: This site is a non-profit fan page voluntarily operated by MasayaUSAPinktiger fans. It is not officially affiliated with Masaya (Masaya Horikawa) or his agency. The copyrights of content such as artist photos and videos used on the site belong to the original copyright holders."
  },
  ja: {
    "nav-home": "ホーム",
    "nav-about": "マサヤについて",
    "nav-schedule": "スケジュール",
    "nav-mascot": "マスコット",
    "nav-goods": "デジタルグッズ",
    "nav-support": "応援＆公式リンク",
    "hero-title": "Masaya USA <span style=\"white-space: nowrap;\">Pinktiger</span> <img src=\"./assets/pinktiger.png?v=4\" alt=\"Pinktiger Mascot\" class=\"custom-hero-mascot emoji-bounce\"><span class=\"emoji-pulse\">💖</span>",
    "hero-subtitle": "マサヤの歌声、雰囲気、そして人間性を感じてください。",
    "hero-tagline": "日本のボーカリスト堀川雅也のグローバルな旅路を応援する全米ファンベースです。",
    "hero-btn-join": "ファンクラブに入る 🎀",
    "hero-btn-learn": "もっと知る 🐯",
    "video-caption": "<span class=\"label\">注目パフォーマンス:</span> 英語字幕付きで歌う「첫눈처럼 너에게 가겠다 (初雪のように君に行く)」 ❄️🎶",
    "about-section-title": "マサヤの歩み 🐯✨",
    "about-bio-h3": "国境を越える歌声",
    "about-bio-p1": "<strong>堀川雅也 (Masaya Horikawa)</strong> は、アジア全域で心を揺さぶるエモーショナルな歌声と圧倒的なステージ存在感を持つ日本の実力派ボーカリストです。完璧なピッチ、伸びやかなハイトーン、そして情熱的な表現力で、歌うすべての楽曲に深い感動をもたらします。",
    "about-bio-p2": "日本のテレビ番組『THEカラオケ★バトル』での活躍や、国境を越えた歌唱バラエティ『日韓歌王戦／日韓トップテンショー』への出演により、一躍トップボーカリストとしての地位を確立。技術だけでなく、ありのままの人間性を込めて心から歌い上げます。",
    "about-quote": "歌は異なる言語、文化、そして魂を繋ぐ架け橋です。一緒にこの音楽の道を歩みましょう！",
    "about-stats-h3": "マサヤを愛する理由",
    "stat-h4-1": "多彩なボーカル",
    "stat-p-1": "パワーバラードから感性豊かなJ-Popカバーまで、彼の響きは唯一無二です。",
    "stat-h4-2": "温かい人柄",
    "stat-p-2": "常に謙虚で親しみやすく、世界中のファンに深く感謝しています。",
    "stat-h4-3": "熱い情熱",
    "stat-p-3": "虎のように、ステージでの彼の決意は目を引き、私たちにインスピレーションを与えます。",
    "stat-h4-4": "リアルタイムの最新情報",
    "stat-p-4": "左のフィードをクリックして、マサヤの最新ニュースをチェックし、ファン同士でシェアしましょう！",
    "gallery-title": "ファンダムギャラリー 🎀",
    "mascot-title": "ピンクタイガーをご紹介！ 🎀🐯",
    "mascot-desc": "全米ファンベースのマスコットとして誕生した**ピンクタイガー**は、アメリカのファンたちのエネルギー、可愛いカワイイ感性、そして大胆な存在感を表現しています。",
    "mascot-label-name": "名前:",
    "mascot-val-name": "ピンクタイガー",
    "mascot-label-skill": "特技:",
    "mascot-val-skill": "マ사야의 고음 샤우팅에 맞춰 춤추기 🎶",
    "mascot-label-acc": "お気に入り:",
    "mascot-val-acc": "水玉ピンクのリボン 🎀",
    "mascot-label-goal": "目標:",
    "mascot-val-goal": "世界中にマサヤへの愛を広げること！ 🌎💖",
    "mascot-bubble": "「ガオー！一緒にマサヤを応援しよう！ 🐯🎀」",
    "channels-title": "Support Masaya",
    "channels-subtitle": "🐯 This is an unofficial, fan-made website created out of pure love to support Masaya. (이곳은 순수한 팬심을 함께 나누는 비공식 팬 사이트입니다. / ここはマサヤを応援する純粋なファン心から作られた非公式ファンサイトです。)",
    "btn-yt-main": "Masaya's YouTube",
    "btn-yt-sub": "カバー動画＆日常Vlogはこちら",
    "btn-ig-main": "Masaya's Instagram",
    "btn-ig-sub": "日常写真や最新情報はこちら",
    "btn-sp-main": "Spotify アーティストページ",
    "btn-sp-sub": "オリジナル曲を配信中",
    "btn-tk-main": "TikTok チャンネル",
    "btn-tk-sub": "ショート歌唱動画はこちら",
    "fan-actions-title": "ファンとしてできること：",
    "fan-action-p-1": "TikTokやShortsで動画クリップを共有する",
    "fan-action-p-2": "英語・日本語・韓国語で応援コメントを残す",
    "fan-action-p-3": "プレイリストにマサヤのカバー曲を追加する",
    "fan-action-p-4": "USA Pinktigerのコミュニティに参加する",
    "footer-desc": "日本の実力派ボーカリスト堀川雅也の全米ファンが繋がるグローバルなコミュニティを作ります。",
    "footer-copyright": "© 2026 Masaya USA Pinktiger - 愛を込めて作られた非公式ファンベース 💖",
    "footer-disclaimer": "🐯 免責事項：当サイトはMasayaUSAPinktigerのファンが自発的に運営する非営利のファンサイトです。マサヤ（堀川マサヤ）氏および所属事務所とは一切公式な関係はありません。サイト内で使用されているアーティストの写真や動画などのコンテンツの著作権は、それぞれの原著作者に帰属します。",
    "disclaimer-text": "🐯 免責事項：当サイトはMasayaUSAPinktigerのファンが自発的に運営する非営利のファンサイトです。マサヤ（堀川マサヤ）氏および所属事務所とは一切公式な関係はありません。サイト内で使用されているアーティストの写真や動画などのコンテンツの著作権は、それぞれの原著作者に帰属します。",
    "fanletters-desc": "マサヤに素敵なメッセージを残そう！ログイン不要です。ポジティブな気持ちとピンクタイガーのエネルギーだけ。 🐯💖",
    "fanletters-nickname": "<i class=\"fa-solid fa-user\"></i> ニックネーム",
    "fanletters-country": "<i class=\"fa-solid fa-earth-americas\"></i> 国名",
    "fanletters-message": "<i class=\"fa-solid fa-envelope-open-text\"></i> メッセージ",
    "fanletters-nickname-ph": "あなたの可愛いニックネーム",
    "fanletters-country-ph": "どこから来ましたか？",
    "fanletters-message-ph": "マサヤに短い手紙を書く...",
    "fanletters-send": "<i class=\"fa-solid fa-paper-plane\"></i> 手紙を送る",
    "fanletters-empty": "最初のメッセージを残そう！ 💌",
    "recent-letters-title": "ピンクタイガーからの最近の手紙 💌",
    "community-btn": "ファンレターを送る",
    "map-title": "マサヤのグローバルフットプリント 🗺️",
    "map-subtitle": "世界中のファンを発見しよう！",
    "map-modal-title": "マーカーを残す 🐾",
    "map-modal-msg-ph": "応援メッセージを書いてね！",
    "map-modal-file-label": "写真をアップロード：",
    "map-modal-submit": "送信",
    "map-modal-cancel": "キャンセル",
    "lbl-location": "位置",
    "lbl-time": "投稿時間",
    "clickMascotHint": "ピンクタイガーのマスコットをクリックして、マサヤの隠し写真を見つけてください！ 🐯✨",
    "ig-updates-title": "マサヤの最新情報 🐯✨",
    "teaser_text": "Pinktigerが楽しいMasayaの足跡巡りゲームを一生懸命作っています！最高得点を記録した方が上位にランクインするシステムも準備中ですので、ぜひご期待ください！",
    "nav-tiger-crush": "Tiger Crush",
    "tiger-crush-title": "Tiger Crush 🐯 🐾<span style=\"color: pink;\">🐾</span>",
    "stamp-tour-desc": "マサヤの足跡をたどろう！クイズに答えてスタンプを集め、次の目的地をアンロック！",
    "tiger-crush-subtitle": "マサヤファンダムのための無料ゲームをお楽しみください！",
    "schedule-title": "マサヤのスケジュール 📅",
    "sch-title-1": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-1": "名古屋 @JAMMIN'",
    "sch-title-2": "「陽炎」 デジタルシングルリリース",
    "sch-loc-2": "オンライン",
    "sch-title-3": "Masaya「人生の名曲に喝采を！コンサート」",
    "sch-loc-3": "@YCC県民文化ホール",
    "sch-title-4": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-4": "東京 @SHIBUYA DIVE",
    "goods-store-title": "Masaya フリーグッズ ストア",
    "goods-store-subtitle": "お気に入りのグッズをダウンロード！",
    "download-btn": "ダウンロード",
    "goods-title-1": "ネイルアート エディション",
    "goods-title-2": "浴衣 エディション",
    "goods-title-3": "ライブ衣装 エディション",
    "goods-title-4": "インスタライブ エディション",
    "goods-title-5": "ラグジュアリー香水 エディション",
    "footer-disclaimer": "🐯 免責事項：当サイトはMasayaUSAPinktigerのファンが自発的に運営する非営利のファンサイトです。マサヤ（堀川マサヤ）氏および所属事務所とは一切公式な関係はありません。サイト内で使用されているアーティストの写真や動画などのコンテンツの著作権は、それぞれの原著作者に帰属します。"
  },
  ko: {
    "nav-home": "홈",
    "nav-about": "마사야 소개",
    "nav-schedule": "주요 스케줄",
    "nav-mascot": "마스코트",
    "nav-goods": "디지털 굿즈",
    "nav-support": "응원 & 공식 채널",
    "hero-title": "Masaya USA <span style=\"white-space: nowrap;\">Pinktiger</span> <img src=\"./assets/pinktiger.png?v=4\" alt=\"Pinktiger Mascot\" class=\"custom-hero-mascot emoji-bounce\"><span class=\"emoji-pulse\">💖</span>",
    "hero-subtitle": "마사야의 목소리, 분위기, 그리고 인간미를 느껴보세요.",
    "hero-tagline": "일본의 보컬리스트 호리카와 마사야의 글로벌 여정을 응원하는 미국 팬베이스입니다.",
    "hero-btn-join": "팬클럽 가입하기 🎀",
    "hero-btn-learn": "자세히 보기 🐯",
    "video-caption": "<span class=\"label\">주요 무대:</span> 영어 자막과 함께 감상하는 마사야의 \"첫눈처럼 너에게 가겠다\" ❄️🎶",
    "video-caption-2": "<span class=\"label\" style=\"font-weight: 700; color: #FF1493;\">For You (영어 자막) - Edited by Pinktiger USA</span>",
    "about-section-title": "마사야의 여정 🐯✨",
    "about-bio-h3": "국경을 넘는 목소리",
    "about-bio-p1": "<strong>호리카와 마사야(堀川雅야)</strong>는 아시아 전역에서 감성적인 가창력과 압도적인 무대 매너로 사랑받는 일본의 실력파 보컬리스트입니다. 완벽한 음정, 가슴을 울리는 고음, 그리고 폭발적인 가창력으로 노래하는 매 순간 깊은 감동을 선사합니다.",
    "about-bio-p2": "일본의 인기 가창 프로그램 『THE 가라오케☆배틀』에서의 대활약과 국경을 넘어 큰 인기를 끈 MBN 방송 『한일가왕전 / 한일톱텐쇼』에 참가하여 한국에서도 큰 화제를 불러일으켰습니다. 뛰어난 가창 기교뿐만 아니라 노래에 진심 어린 인간미를 듬뿍 담아 부릅니다.",
    "about-quote": "노래는 다른 언어, 문화, 그리고 영혼을 잇는 가교입니다. 이 아름다운 음악의 여정을 함께 걸어갑시다!",
    "about-stats-h3": "마사야를 사랑하는 이유",
    "stat-h4-1": "다채로운 보컬",
    "stat-p-1": "파워 발라드부터 감성적인 J-Pop 커버까지 독보적인 울림을 선사합니다.",
    "stat-h4-2": "따뜻한 성품",
    "stat-p-2": "언제나 겸손하고 팬들과 깊이 소통하며 글로벌 팬들을 아낍니다.",
    "stat-h4-3": "뜨거운 열정",
    "stat-p-3": "호랑이처럼 무대 위에서의 결연한 의지와 폭발적인 열정으로 영감을 줍니다.",
    "stat-h4-4": "실시간 소식 공유",
    "stat-p-4": "왼쪽 피드를 클릭해 마사야의 가장 최신 소식을 확인하고 팬들과 함께 공유해 보세요!",
    "gallery-title": "팬덤 갤러리 🎀",
    "mascot-title": "핑크타이거를 소개합니다! 🎀🐯",
    "mascot-desc": "미국 팬베이스의 마스코트인 **핑크타이거**는 미국 팬들의 열정적인 에너지, 귀여운 카와이 감성, 그리고 대담한 매력을 상징합니다.",
    "mascot-label-name": "이름:",
    "mascot-val-name": "핑크타이거",
    "mascot-label-skill": "특기:",
    "mascot-val-skill": "마사야의 고음 샤우팅에 맞춰 춤추기 🎶",
    "mascot-label-acc": "최애 아이템:",
    "mascot-val-acc": "땡땡이 핑크 리본 🎀",
    "mascot-label-goal": "목표:",
    "mascot-val-goal": "전 세계에 마사야의 매력 전파하기! 🌎💖",
    "mascot-bubble": "“어흥! 우리 같이 마사야를 응원해요! 🐯🎀”",
    "channels-title": "Support Masaya",
    "channels-subtitle": "🐯 This is an unofficial, fan-made website created out of pure love to support Masaya. (이곳은 순수한 팬심을 함께 나누는 비공식 팬 사이트입니다. / ここはマサヤを応援する純粋なファン心から作られた非公式ファンサイトです。)",
    "btn-yt-main": "Masaya's YouTube",
    "btn-yt-sub": "커버 영상 및 일상 브이로그 구독",
    "btn-ig-main": "Masaya's Instagram",
    "btn-ig-sub": "일상 사진 및 최신 정보 확인",
    "btn-sp-main": "스포티파이 아티스트 페이지",
    "btn-sp-sub": "오리지널 음원 스트리밍",
    "btn-tk-main": "틱톡 채널",
    "btn-tk-sub": "노래 숏폼 영상 감상",
    "fan-actions-title": "팬으로서 함께하는 방법:",
    "fan-action-p-1": "틱톡 및 쇼츠에 무대 영상 공유하기",
    "fan-action-p-2": "영문/일문/국문으로 따뜻한 응원 댓글 달기",
    "fan-action-p-3": "마사야의 노래들을 개인 플레이리스트에 담기",
    "fan-action-p-4": "USA Pinktiger 소통 공간 함께 참여하기",
    "footer-desc": "일본의 보컬 신성 호리카와 마사야의 미국 팬들이 모이는 글로벌 소통 커뮤니티입니다.",
    "footer-copyright": "© 2026 Masaya USA Pinktiger - 사랑으로 만든 비공식 팬베이스 💖",
    "footer-disclaimer": "🐯 면책 조항: 본 사이트는 MasayaUSAPinktiger 팬들이 자발적으로 운영하는 비영리 팬페이지입니다. 마사야(호리카와 마사야) 님 및 소속사와는 어떠한 공식적인 관련도 없으며, 사이트 내 사용된 아티스트의 사진 및 영상 등 콘텐츠의 저작권은 원저작권자에게 있습니다.",
    "disclaimer-text": "🐯 면책 조항: 본 사이트는 MasayaUSAPinktiger 팬들이 자발적으로 운영하는 비영리 팬페이지입니다. 마사야(호리카와 마사야) 님 및 소속사와는 어떠한 공식적인 관련도 없으며, 사이트 내 사용된 아티스트의 사진 및 영상 등 콘텐츠의 저작권은 원저작권자에게 있습니다.",
    "fanletters-desc": "마사야에게 따뜻한 메시지를 남겨보세요! 로그인 불필요. 긍정적인 마음과 핑크 타이거 에너지면 충분해요. 🐯💖",
    "fanletters-nickname": "<i class=\"fa-solid fa-user\"></i> 닉네임",
    "fanletters-country": "<i class=\"fa-solid fa-earth-americas\"></i> 국가",
    "fanletters-message": "<i class=\"fa-solid fa-envelope-open-text\"></i> 메시지",
    "fanletters-nickname-ph": "당신의 귀여운 닉네임",
    "fanletters-country-ph": "어디서 오셨나요?",
    "fanletters-message-ph": "마사야에게 짧은 편지를 써주세요...",
    "fanletters-send": "<i class=\"fa-solid fa-paper-plane\"></i> 편지 보내기",
    "fanletters-empty": "첫 번째로 메시지를 남겨보세요! 💌",
    "recent-letters-title": "핑크타이거의 최근 편지 💌",
    "community-btn": "팬레터 보내기",
    "map-title": "마사야의 글로벌 발자취 🗺️",
    "map-subtitle": "전 세계의 팬들을 발견해보세요!",
    "map-modal-title": "마커 남기기 🐾",
    "map-modal-msg-ph": "응원 메시지를 남겨주세요!",
    "map-modal-file-label": "사진 업로드:",
    "map-modal-submit": "제출",
    "map-modal-cancel": "취소",
    "lbl-location": "위치",
    "lbl-time": "작성 시간",
    "clickMascotHint": "핑크타이거 마스코트를 클릭해서 마사야의 숨은 사진을 찾으세요! 🐯✨",
    "ig-updates-title": "마사야 최신 소식 🐯✨",
    "teaser_text": "핑크타이거가 신나는 마사야 발자취 투어 게임을 열심히 뚝딱뚝딱 만들고 있어요! 최고점을 기록하신 분이 상위에 올라가는 랭킹 시스템도 준비 중이니 기대해 주세요!",
    "nav-tiger-crush": "Tiger Crush",
    "tiger-crush-title": "Tiger Crush 🐯 🐾<span style=\"color: pink;\">🐾</span>",
    "stamp-tour-desc": "마사야의 발자취를 따라가보세요! 퀴즈를 풀고 스탬프를 모아 다음 목적지를 잠금 해제하세요!",
    "tiger-crush-subtitle": "마사야 팬덤을 위한 무료 게임을 즐겨보세요!",
    "schedule-title": "마사야의 스케줄 📅",
    "sch-title-1": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-1": "나고야 @JAMMIN'",
    "sch-title-2": "'아지랑이(陽炎)' 디지털 싱글 발매",
    "sch-loc-2": "온라인",
    "sch-title-3": "마사야 '인생의 명곡에 갈채를! 콘서트'",
    "sch-loc-3": "@YCC 현민 문화홀",
    "sch-title-4": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-4": "도쿄 @SHIBUYA DIVE",
    "goods-store-title": "Masaya Free Goods Store",
    "goods-store-subtitle": "원하시는 굿즈를 다운로드하세요!",
    "download-btn": "다운로드",
    "goods-title-1": "네일아트 에디션",
    "goods-title-2": "유카타 에디션",
    "goods-title-3": "콘서트 제복 에디션",
    "goods-title-4": "인스타 라이브 에디션",
    "goods-title-5": "럭셔리 향수 앰버서더 에디션",
    "footer-disclaimer": "🐯 면책 조항: 본 사이트는 MasayaUSAPinktiger 팬들이 자발적으로 운영하는 비영리 팬페이지입니다. 마사야(호리카와 마사야) 님 및 소속사와는 어떠한 공식적인 관련도 없으며, 사이트 내 사용된 아티스트의 사진 및 영상 등 콘텐츠의 저작권은 원저작권자에게 있습니다."
  },
  es: {
    "nav-home": "Inicio",
    "nav-about": "Sobre Masaya",
    "nav-schedule": "Próximos Eventos",
    "nav-mascot": "Nuestra Mascota",
    "nav-goods": "Productos Digitales",
    "nav-support": "Apoyo y Enlaces",
    "hero-title": "Masaya USA <span style=\"white-space: nowrap;\">Pinktiger</span> <img src=\"./assets/pinktiger.png?v=4\" alt=\"Pinktiger Mascot\" class=\"custom-hero-mascot emoji-bounce\"><span class=\"emoji-pulse\">💖</span>",
    "hero-subtitle": "Experimenta la voz, la vibra y la humanidad de Masaya.",
    "hero-tagline": "La base de fans de EE. UU. dedicada a apoyar el viaje global del vocalista japonés Masaya Horikawa.",
    "hero-btn-join": "Únete a la Manada 🎀",
    "hero-btn-learn": "Aprende Más 🐯",
    "video-caption": "<span class=\"label\">Actuación Destacada:</span> Masaya cantando \"첫눈처럼 너에게 가겠다 (I Will Go to You Like the First Snow)\" con letras en inglés ❄️🎶",
    "video-caption-2": "<span class=\"label\" style=\"font-weight: 700; color: #FF1493;\">For You (Subtítulos en Inglés) - Editado por Pinktiger USA</span>",
    "about-section-title": "El Viaje de Masaya 🐯✨",
    "about-bio-h3": "La Voz que Cruza Fronteras",
    "about-bio-p1": "<strong>Masaya Horikawa (堀川雅也)</strong> es un poderoso vocalista de Japón cuyo rango emocional y brillante presencia escénica han capturado corazones en toda Asia. Conocido por su afinación perfecta, sus impresionantes notas altas y sus entregas apasionadas, aporta un profundo sentido de vulnerabilidad a cada canción que interpreta.",
    "about-bio-p2": "Su participación en programas de canto de alto perfil como <em>THE Karaoke ☆ Battle</em> de Japón y la sensación televisiva transfronteriza <em>Han-Il King of Song / Korea-Japan Top Ten Show</em> lo establecieron como un vocalista de primer nivel. Masaya actúa no solo con técnica, sino con todo su corazón, compartiendo su humanidad cruda a través de cada letra.",
    "about-quote": "Cantar es el puente que conecta diferentes idiomas, culturas y almas. ¡Caminemos juntos por este camino musical!",
    "about-stats-h3": "Por Qué Amamos a Masaya",
    "stat-h4-1": "Versatilidad Vocal",
    "stat-p-1": "Desde power ballads hasta emotivos covers de J-Pop, su resonancia es inigualable.",
    "stat-h4-2": "Personalidad Cálida",
    "stat-p-2": "Siempre humilde, conectado y profundamente agradecido con su base de fans global.",
    "stat-h4-3": "Pasión Feroz",
    "stat-p-3": "Como un tigre, su determinación en el escenario exige atención e inspira.",
    "stat-h4-4": "Comparte Noticias en Vivo",
    "stat-p-4": "¡Haz clic en el feed de la izquierda para ver las últimas novedades de Masaya y compártelas con los fans!",
    "gallery-title": "Galería del Fandom 🎀",
    "mascot-title": "¡Conoce a Pinktiger! 🎀🐯",
    "mascot-desc": "Creada como la mascota de la Fanbase de EE. UU., **Pinktiger** representa la energía, la estética kawaii y la presencia audaz de los fans americanos de Masaya.",
    "mascot-label-name": "Nombre:",
    "mascot-val-name": "Pinktiger",
    "mascot-label-skill": "Habilidad Especial:",
    "mascot-val-skill": "Rebotar con las notas altas de Masaya 🎶",
    "mascot-label-acc": "Accesorio Favorito:",
    "mascot-val-acc": "Lazo rosa con lunares 🎀",
    "mascot-label-goal": "Objetivo:",
    "mascot-val-goal": "¡Difundir el amor por Masaya en todo el mundo! 🌎💖",
    "mascot-bubble": "\"¡Roar! ¡Animemos a Masaya juntos! 🐯🎀\"",
    "channels-title": "Apoya a Masaya",
    "channels-subtitle": "🐯 Este es un sitio web no oficial hecho por fans con puro amor para apoyar a Masaya. (이곳은 순수한 팬심을 함께 나누는 비공식 팬 사이트입니다. / ここはマサヤを応援する純粋なファン心から作られた非公式ファンサイトです。)",
    "btn-yt-main": "YouTube de Masaya",
    "btn-yt-sub": "Suscríbete para Covers y Vlogs",
    "btn-ig-main": "Instagram de Masaya",
    "btn-ig-sub": "Sigue su vida diaria y novedades",
    "btn-sp-main": "Página de Artista en Spotify",
    "btn-sp-sub": "Reproduce música original",
    "btn-tk-main": "Canal de TikTok",
    "btn-tk-sub": "Mira videos musicales cortos",
    "fan-actions-title": "Cómo Ayudar como Fan:",
    "fan-action-p-1": "Comparte videos en TikTok y Shorts",
    "fan-action-p-2": "Deja comentarios de apoyo en EN/JA/KO/ES",
    "fan-action-p-3": "Agrega los covers de Masaya a tus listas",
    "fan-action-p-4": "Únete a las discusiones de USA Pinktiger",
    "footer-desc": "Creando un hogar global para los fans en EE. UU. de la sensación vocal japonesa Masaya Horikawa.",
    "footer-copyright": "© 2026 Masaya USA Pinktiger - Fanbase no oficial construida con amor. 💖",
    "footer-disclaimer": "🐯 Descargo de responsabilidad: Este sitio es una página de fans sin fines de lucro operada voluntariamente por fans de MasayaUSAPinktiger. No está afiliada oficialmente con Masaya (Masaya Horikawa) ni con su agencia. Los derechos de autor del contenido, como fotos y videos del artista utilizados en el sitio, pertenecen a los titulares de los derechos de autor originales.",
    "disclaimer-text": "🐯 Descargo de responsabilidad: Este sitio es una página de fans sin fines de lucro operada voluntariamente por fans de MasayaUSAPinktiger. No está afiliada oficialmente con Masaya (Masaya Horikawa) ni con su agencia. Los derechos de autor del contenido, como fotos y videos del artista utilizados en el sitio, pertenecen a los titulares de los derechos de autor originales.",
    "fanletters-desc": "¡Deja un hermoso mensaje para Masaya! No se requiere iniciar sesión. Solo vibras positivas y energía de pink tiger. 🐯💖",
    "fanletters-nickname": "<i class=\"fa-solid fa-user\"></i> Apodo",
    "fanletters-country": "<i class=\"fa-solid fa-earth-americas\"></i> País",
    "fanletters-message": "<i class=\"fa-solid fa-envelope-open-text\"></i> Mensaje",
    "fanletters-nickname-ph": "Tu lindo apodo",
    "fanletters-country-ph": "¿De dónde eres?",
    "fanletters-message-ph": "Escribe una carta corta para Masaya...",
    "fanletters-send": "<i class=\"fa-solid fa-paper-plane\"></i> Enviar Carta",
    "fanletters-empty": "¡Sé el primero en dejar un mensaje! 💌",
    "recent-letters-title": "Cartas Recientes de los Pinktigers 💌",
    "community-btn": "Cartas de Fans desde Lejos",
    "map-title": "Huella Global de Masaya 🗺️",
    "map-subtitle": "¡Descubre fans alrededor del mundo!",
    "map-modal-title": "Deja un Marcador 🐾",
    "map-modal-msg-ph": "¡Escribe un mensaje de ánimo!",
    "map-modal-file-label": "Sube una Foto:",
    "map-modal-submit": "Enviar",
    "map-modal-cancel": "Cancelar",
    "lbl-location": "Ubicación",
    "lbl-time": "Tiempo",
    "clickMascotHint": "¡Haz clic en la mascota Pinktiger para encontrar fotos ocultas de Masaya! 🐯✨",
    "ig-updates-title": "Últimas noticias de Masaya 🐯✨",
    "teaser_text": "¡Nuestro Pinktiger está trabajando duro para traerte un divertido juego de recorrido por las huellas de Masaya! Además, ¡espera nuestro sistema de clasificación donde los mejores puntajes aparecerán en la tabla de líderes!",
    "nav-tiger-crush": "Tiger Crush",
    "tiger-crush-title": "Tiger Crush 🐯 🐾<span style=\"color: pink;\">🐾</span>",
    "stamp-tour-desc": "¡Sigue las huellas de Masaya! ¡Responde cuestionarios para coleccionar sellos y desbloquear el siguiente destino!",
    "tiger-crush-subtitle": "¡Disfruta de tu juego gratuito para el fandom de Masaya!",
    "schedule-title": "Horario de Masaya 📅",
    "sch-title-1": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-1": "Nagoya @JAMMIN'",
    "sch-title-2": "Lanzamiento del Sencillo Digital \"Kagero\"",
    "sch-loc-2": "En línea",
    "sch-title-3": "Concierto de Masaya \"¡Aplausos a las Obras Maestras de la Vida!\"",
    "sch-loc-3": "@YCC Prefectural Cultural Hall",
    "sch-title-4": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-4": "Tokio @SHIBUYA DIVE",
    "goods-store-title": "Tienda de Productos Gratuitos de Masaya",
    "goods-store-subtitle": "¡Descarga tus productos favoritos!",
    "download-btn": "Descargar",
    "goods-title-1": "Edición Nail Art",
    "goods-title-2": "Edición Yukata",
    "goods-title-3": "Edición Atuendo de Concierto",
    "goods-title-4": "Edición Insta Live",
    "goods-title-5": "Edición Embajador de Perfume de Lujo",
    "footer-disclaimer": "🐯 Descargo de responsabilidad: Este sitio es una página de fans sin fines de lucro operada voluntariamente por fans de MasayaUSAPinktiger. No está afiliada oficialmente con Masaya (Masaya Horikawa) ni con su agencia. Los derechos de autor del contenido, como fotos y videos del artista utilizados en el sitio, pertenecen a los titulares de los derechos de autor originales."
  },
  fr: {
    "nav-home": "Accueil",
    "nav-about": "À propos de Masaya",
    "nav-schedule": "Événements à venir",
    "nav-mascot": "Notre Mascotte",
    "nav-goods": "Produits Numériques",
    "nav-support": "Soutien et Chaînes",
    "nav-video": "Archives Vidéo",
    "hero-title": "Masaya USA <span style=\"white-space: nowrap;\">Pinktiger</span> <img src=\"./assets/pinktiger.png?v=4\" alt=\"Pinktiger Mascot\" class=\"custom-hero-mascot emoji-bounce\"><span class=\"emoji-pulse\">💖</span>",
    "hero-subtitle": "Découvrez la voix, l'ambiance et l'humanité de Masaya.",
    "hero-tagline": "La base de fans américaine dédiée à soutenir le parcours mondial du chanteur japonais Masaya Horikawa.",
    "hero-btn-join": "Rejoignez la Fierté 🎀",
    "hero-btn-learn": "En Savoir Plus 🐯",
    "video-caption": "<span class=\"label\">Performance en Vedette :</span> Masaya chantant \"첫눈처럼 너에게 가겠다 (I Will Go to You Like the First Snow)\" avec des paroles en anglais ❄️🎶",
    "video-caption-2": "<span class=\"label\" style=\"font-weight: 700; color: #FF1493;\">Pour Vous (Sous-titres anglais) - Édité par Pinktiger USA</span>",
    "about-section-title": "Le Parcours de Masaya 🐯✨",
    "about-bio-h3": "La Voix qui Traverse les Frontières",
    "about-bio-p1": "<strong>Masaya Horikawa (堀川雅也)</strong> est un puissant chanteur japonais dont la gamme émotionnelle et la présence scénique lumineuse ont conquis les cœurs à travers l'Asie. Connu pour sa justesse parfaite, ses notes hautes époustouflantes et ses interprétations passionnées, il apporte un profond sentiment de vulnérabilité à chaque chanson.",
    "about-bio-p2": "Sa participation à des émissions de chant telles que <em>THE Karaoke ☆ Battle</em> au Japon et <em>Han-Il King of Song / Korea-Japan Top Ten Show</em> l'a établi comme un chanteur de premier ordre. Masaya ne chante pas seulement avec technique, mais avec tout son cœur, partageant son humanité brute.",
    "about-quote": "Chanter est le pont qui relie différentes langues, cultures et âmes. Marchons ensemble sur ce chemin musical !",
    "about-stats-h3": "Pourquoi Nous Aimons Masaya",
    "stat-h4-1": "Polyvalence Vocale",
    "stat-p-1": "Des ballades puissantes aux reprises émouvantes de J-Pop, sa résonance est inégalée.",
    "stat-h4-2": "Personnalité Chaleureuse",
    "stat-p-2": "Toujours humble, connecté et profondément reconnaissant envers sa base de fans mondiale.",
    "stat-h4-3": "Passion Féroce",
    "stat-p-3": "Comme un tigre, sa détermination sur scène force le respect et inspire.",
    "stat-h4-4": "Partage d'Actualités en Direct",
    "stat-p-4": "Cliquez sur le flux à gauche pour découvrir les dernières nouvelles de Masaya et partagez-les avec d'autres fans !",
    "gallery-title": "Galerie du Fandom 🎀",
    "mascot-title": "Rencontrez Pinktiger ! 🎀🐯",
    "mascot-desc": "Créée comme mascotte de la Fanbase américaine, **Pinktiger** représente l'énergie, l'esthétique kawaii et la présence audacieuse des fans américains de Masaya.",
    "mascot-label-name": "Nom :",
    "mascot-val-name": "Pinktiger",
    "mascot-label-skill": "Compétence Spéciale :",
    "mascot-val-skill": "Rebondir sur les notes hautes de Masaya 🎶",
    "mascot-label-acc": "Accessoire Préféré :",
    "mascot-val-acc": "Nœud rose à pois 🎀",
    "mascot-label-goal": "Objectif :",
    "mascot-val-goal": "Répandre l'amour de Masaya dans le monde entier ! 🌎💖",
    "mascot-bubble": "\"Roar ! Encourageons Masaya ensemble ! 🐯🎀\"",
    "channels-title": "Soutenez Masaya",
    "channels-subtitle": "🐯 Ceci est un site de fans non officiel créé par pur amour pour soutenir Masaya. (이곳은 순수한 팬심을 함께 나누는 비공식 팬 사이트입니다. / ここはマサヤを応援する純粋なファン心から作られた非公式ファンサイトです。)",
    "btn-yt-main": "YouTube de Masaya",
    "btn-yt-sub": "Abonnez-vous pour des Reprises & Vlogs",
    "btn-ig-main": "Instagram de Masaya",
    "btn-ig-sub": "Suivez son quotidien et ses actualités",
    "btn-sp-main": "Page Artiste Spotify",
    "btn-sp-sub": "Écoutez sa musique originale",
    "btn-tk-main": "Chaîne TikTok",
    "btn-tk-sub": "Regardez de courts clips musicaux",
    "fan-actions-title": "Comment Aider en tant que Fan :",
    "fan-action-p-1": "Partagez des TikToks et des Shorts",
    "fan-action-p-2": "Laissez des commentaires de soutien en EN/JA/KO/ES",
    "fan-action-p-3": "Ajoutez les reprises de Masaya à vos playlists",
    "fan-action-p-4": "Rejoignez les discussions de USA Pinktiger",
    "footer-desc": "Créer un foyer mondial pour les fans américains de la sensation vocale japonaise Masaya Horikawa.",
    "footer-copyright": "© 2026 Masaya USA Pinktiger - Fanbase non officielle construite avec amour. 💖",
    "footer-disclaimer": "🐯 Clause de non-responsabilité : Ce site est une page de fans à but non lucratif gérée bénévolement par les fans de MasayaUSAPinktiger. Il n'est pas officiellement affilié à Masaya (Masaya Horikawa) ni à son agence. Les droits d'auteur des contenus tels que les photos et vidéos de l'artiste utilisés sur le site appartiennent aux titulaires des droits d'auteur originaux.",
    "disclaimer-text": "🐯 Clause de non-responsabilité : Ce site est une page de fans à but non lucratif gérée bénévolement par les fans de MasayaUSAPinktiger. Il n'est pas officiellement affilié à Masaya (Masaya Horikawa) ni à son agence. Les droits d'auteur des contenus tels que les photos et vidéos de l'artiste utilisés sur le site appartiennent aux titulaires des droits d'auteur originaux.",
    "fanletters-desc": "Laissez un joli message pour Masaya ! Pas besoin de se connecter. Seulement de bonnes ondes et l'énergie du tigre rose. 🐯💖",
    "fanletters-nickname": "<i class=\"fa-solid fa-user\"></i> Pseudo",
    "fanletters-country": "<i class=\"fa-solid fa-earth-americas\"></i> Pays",
    "fanletters-message": "<i class=\"fa-solid fa-envelope-open-text\"></i> Message",
    "fanletters-nickname-ph": "Votre joli pseudo",
    "fanletters-country-ph": "D'où venez-vous ?",
    "fanletters-message-ph": "Écrivez une courte lettre pour Masaya...",
    "fanletters-send": "<i class=\"fa-solid fa-paper-plane\"></i> Envoyer la Lettre",
    "fanletters-empty": "Soyez le premier à laisser un message ! 💌",
    "recent-letters-title": "Lettres Récentes des Pinktigers 💌",
    "community-btn": "Lettres de Fans de Loin",
    "map-title": "Empreinte Mondiale de Masaya 🗺️",
    "map-subtitle": "Découvrez des fans du monde entier !",
    "map-modal-title": "Laissez un Marqueur 🐾",
    "map-modal-msg-ph": "Écrivez un message d'encouragement !",
    "map-modal-file-label": "Téléchargez une Photo :",
    "map-modal-submit": "Soumettre",
    "map-modal-cancel": "Annuler",
    "lbl-location": "Emplacement",
    "lbl-time": "Heure",
    "clickMascotHint": "Cliquez sur la Mascotte Pinktiger pour trouver des photos cachées de Masaya ! 🐯✨",
    "ig-updates-title": "Dernières Mises à Jour de Masaya 🐯✨",
    "teaser_text": "Notre Pinktiger travaille dur pour vous proposer un jeu de parcours amusant sur les traces de Masaya ! De plus, attendez-vous à notre système de classement où les meilleurs scores figureront au sommet du classement !",
    "nav-tiger-crush": "Tiger Crush",
    "tiger-crush-title": "Tiger Crush 🐯 🐾<span style=\"color: pink;\">🐾</span>",
    "stamp-tour-desc": "Suivez les traces de Masaya ! Répondez aux quiz pour collectionner des timbres et débloquer la prochaine destination !",
    "tiger-crush-subtitle": "Profitez de votre jeu gratuit pour le fandom de Masaya !",
    "schedule-title": "Programme de Masaya 📅",
    "sch-title-1": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-1": "Nagoya @JAMMIN'",
    "sch-title-2": "Sortie du Single Numérique \"Kagero\"",
    "sch-loc-2": "En ligne",
    "sch-title-3": "Concert de Masaya \"Acclamations aux Chefs-d'œuvre de la Vie !\"",
    "sch-loc-3": "@YCC Prefectural Cultural Hall",
    "sch-title-4": "AGE LIVE TOUR 2026 \"PERCENTAGE%\"",
    "sch-loc-4": "Tokyo @SHIBUYA DIVE",
    "goods-store-title": "Boutique de Produits Gratuits Masaya",
    "goods-store-subtitle": "Téléchargez vos articles préférés !",
    "download-btn": "Télécharger",
    "goods-title-1": "Édition Art des Ongles",
    "goods-title-2": "Édition Yukata",
    "goods-title-3": "Édition Tenue de Concert",
    "goods-title-4": "Édition Insta Live",
    "goods-title-5": "Édition Ambassadeur de Parfum de Luxe",
    "footer-disclaimer": "🐯 Clause de non-responsabilité : Ce site est une page de fans à but non lucratif gérée bénévolement par les fans de MasayaUSAPinktiger. Il n'est pas officiellement affilié à Masaya (Masaya Horikawa) ni à son agence. Les droits d'auteur des contenus tels que les photos et vidéos de l'artiste utilisés sur le site appartiennent aux titulaires des droits d'auteur originaux."
  }
};

// Profanity Filter Helper
window.containsProfanity = function(text) {
  if (!text) return false;
  const badWords = [
    'fuck', 'shit', 'bitch', 'asshole', 'cunt', 'dick', 'pussy', 'bastard', 'slut', 'whore',
    '씨발', '시발', '병신', '개새끼', '존나', '지랄', '미친', '좆', '창녀', '새끼',
    'puta', 'mierda', 'cabrón', 'pendejo', 'coño', 'joder', 'maricón', 'zorra', 'puto',
    'バカ', 'ばか', 'アホ', 'あほ', '死ね', 'クソ', 'くそ', 'キモい', 'ガイジ', 'カス'
  ];
  const lowerText = text.toLowerCase();
  return badWords.some(word => lowerText.includes(word));
};


// Firebase Initialization (Placeholder Configuration)
const firebaseConfig = {
  // TODO: Replace with actual Firebase config
  apiKey: "AIzaSyDummyKey-xxxxxxxxxxxxxxxxxxx",
  authDomain: "masaya-usa-pinktiger.firebaseapp.com",
  databaseURL: "https://masaya-usa-pinktiger-default-rtdb.firebaseio.com",
  projectId: "masaya-usa-pinktiger",
  storageBucket: "masaya-usa-pinktiger.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};

// Initialize Firebase only if the SDK is loaded
let db = null;
try {
  if (typeof firebase !== 'undefined' && !firebase.apps.length) {
    firebase.initializeApp(firebaseConfig);
  }
  if (typeof firebase !== 'undefined') {
    db = firebase.database();
  }
} catch (error) {
  console.warn("Firebase initialization failed:", error);
}

document.addEventListener('DOMContentLoaded', () => {
  initScrollReveal();
  initSparkleEffect();
  initCuteAudio();
  initLanguageSwitcher();
  initVideoCarousel();
  initMobileMenu();
  initInteractiveMap();
  initGoodsSwiper();

});

/* ==========================================================================
   2. Scroll Reveal Animation (Intersection Observer)
   ========================================================================== */
function initScrollReveal() {
  const sections = document.querySelectorAll('.fade-in-section');
  
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.12 // Trigger when 12% of the element is visible
  };

  const observer = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target); // Stop observing once it's revealed
      }
    });
  }, observerOptions);

  sections.forEach(section => {
    observer.observe(section);
  });
}

/* ==========================================================================
   3. Kawaii Sparkle Particle Effect
   ========================================================================== */
function initSparkleEffect() {
  const container = document.getElementById('sparkle-container');
  const sparkleToggle = document.getElementById('sparkle-toggle');
  
  let particlesEnabled = true;
  let autoSparkleInterval = null;
  const particlesPool = ['✨', '💖', '🐯', '🎀', '⭐', '🌸'];

  // Toggle sparkles on and off
  if (sparkleToggle) {
    sparkleToggle.addEventListener('click', (e) => {
      particlesEnabled = !particlesEnabled;
      
      if (particlesEnabled) {
        sparkleToggle.classList.remove('disabled');
        sparkleToggle.style.opacity = '1';
        startAutoSparkles();
        createSparkleExplosion(e.clientX, e.clientY, 15);
      } else {
        sparkleToggle.classList.add('disabled');
        sparkleToggle.style.opacity = '0.5';
        stopAutoSparkles();
      }
    });
  }

  // Spawn sparkle explosion on click
  window.addEventListener('click', (e) => {
    if (!particlesEnabled) return;
    
    // Do not spawn on buttons or links to prevent clutter, except we spawn custom explosions
    if (e.target.tagName === 'A' || e.target.tagName === 'BUTTON' || e.target.closest('a') || e.target.closest('button')) {
      createSparkleExplosion(e.clientX, e.clientY, 12);
    } else {
      createSparkleExplosion(e.clientX, e.clientY, 6);
    }
  });

  function createSparkleExplosion(x, y, count) {
    for (let i = 0; i < count; i++) {
      createParticle(x, y);
    }
  }

  function createParticle(x, y) {
    const particle = document.createElement('div');
    particle.className = 'sparkle-particle';
    
    // Pick random symbol
    const symbol = particlesPool[Math.floor(Math.random() * particlesPool.length)];
    particle.innerText = symbol;

    // Randomize movement angles and distance
    const angle = Math.random() * Math.PI * 2;
    const distance = 40 + Math.random() * 80;
    const dx = Math.cos(angle) * distance;
    const dy = Math.sin(angle) * distance;
    
    // Set styles
    particle.style.left = `${x}px`;
    particle.style.top = `${y}px`;
    particle.style.setProperty('--dx', `${dx}px`);
    particle.style.setProperty('--dy', `${dy}px`);
    
    // Randomize rotation speed and initial rotation
    particle.style.transform = `translate(0, 0) scale(0) rotate(${Math.random() * 360}deg)`;

    container.appendChild(particle);

    // Clean up particle element after animation completes
    setTimeout(() => {
      particle.remove();
    }, 1500);
  }

  // Automatic background sparkles drifting down
  function startAutoSparkles() {
    autoSparkleInterval = setInterval(() => {
      const rx = Math.random() * window.innerWidth;
      const ry = Math.random() * window.innerHeight;
      createSparkleExplosion(rx, ry, 1);
    }, 2000);
  }

  function stopAutoSparkles() {
    if (autoSparkleInterval) {
      clearInterval(autoSparkleInterval);
    }
  }

  // Start the background sparkles automatically
  startAutoSparkles();
}

/* ==========================================================================
   4. Cute Synthesizer Audio (Web Audio API)
   ========================================================================== */
function initCuteAudio() {
  const soundBtn = document.getElementById('sound-btn');
  
  if (soundBtn) {
    soundBtn.addEventListener('click', () => {
      playCuteChime();
      
      // Visual button action feedback
      soundBtn.classList.add('playing');
      setTimeout(() => soundBtn.classList.remove('playing'), 300);
    });
  }
}

function playCuteChime() {
  try {
    const AudioContext = window.AudioContext || window.webkitAudioContext;
    if (!AudioContext) return;
    
    const audioCtx = new AudioContext();
    
    // Sequence notes frequencies (J-pop game chime chords: C5, E5, G5, C6)
    const notes = [
      { freq: 523.25, timeOffset: 0, duration: 0.12 },   // C5
      { freq: 659.25, timeOffset: 0.08, duration: 0.12 },  // E5
      { freq: 783.99, timeOffset: 0.16, duration: 0.12 },  // G5
      { freq: 1046.50, timeOffset: 0.24, duration: 0.25 }  // C6
    ];

    const now = audioCtx.currentTime;

    notes.forEach(note => {
      const oscillator = audioCtx.createOscillator();
      const gainNode = audioCtx.createGain();

      oscillator.type = 'sine'; // Soft rounded sound, like a bubble chime
      oscillator.frequency.setValueAtTime(note.freq, now + note.timeOffset);

      // Sweet fadeout envelope
      gainNode.gain.setValueAtTime(0, now + note.timeOffset);
      gainNode.gain.linearRampToValueAtTime(0.12, now + note.timeOffset + 0.02);
      gainNode.gain.exponentialRampToValueAtTime(0.001, now + note.timeOffset + note.duration);

      oscillator.connect(gainNode);
      gainNode.connect(audioCtx.destination);

      oscillator.start(now + note.timeOffset);
      oscillator.stop(now + note.timeOffset + note.duration);
    });

  } catch (error) {
    console.warn('Web Audio API not supported or blocked by permissions:', error);
  }
}

/* ==========================================================================
   5. Language Switcher & Localization Logic
   ========================================================================== */
function initLanguageSwitcher() {
  const langButtons = document.querySelectorAll('.lang-btn');
  
  // Set default language from localStorage or URL hash or browser setting
  let currentLang = 'en';
  const urlHash = window.location.hash.replace('#', '');
  const savedLang = localStorage.getItem('preferredLang');
  
  if (window.translations[urlHash]) {
    currentLang = urlHash;
  } else if (window.translations[savedLang]) {
    currentLang = savedLang;
  } else {
    // Check browser default language (en, ja, ko)
    const browserLang = navigator.language.slice(0, 2);
    if (window.translations[browserLang]) {
      currentLang = browserLang;
    }
  }

  // Update layout immediately
  updateLanguage(currentLang);

  // Set event listeners for language selection buttons
  langButtons.forEach(btn => {
    btn.addEventListener('click', (e) => {
      // Fetch selected language
      const selectedLang = btn.getAttribute('data-lang');
      if (window.translations[selectedLang]) {
        updateLanguage(selectedLang);
        playCuteLanguageChime();
      }
    });
  });

  // Listen to hash change events for seamless anchor link transitions
  window.addEventListener('hashchange', () => {
    const hash = window.location.hash.replace('#', '');
    if (window.translations[hash]) {
      updateLanguage(hash);
    }
  });
}

function updateLanguage(lang) {
  // Save chosen language preference
  localStorage.setItem('preferredLang', lang);
  
  // Set HTML lang attribute
  document.documentElement.lang = lang;
  
  // Update class of lang switcher buttons
  const langButtons = document.querySelectorAll('.lang-btn');
  langButtons.forEach(btn => {
    if (btn.getAttribute('data-lang') === lang) {
      btn.classList.add('active');
    } else {
      btn.classList.remove('active');
    }
  });

  // Translate all marked elements
  const translateElements = document.querySelectorAll('[data-translate]');
  translateElements.forEach(el => {
    const key = el.getAttribute('data-translate');
    if (window.translations[lang] && window.translations[lang][key]) {
      el.innerHTML = window.translations[lang][key];
    }
  });

  // Translate placeholders
  const translatePlaceholders = document.querySelectorAll('[data-translate-placeholder]');
  translatePlaceholders.forEach(el => {
    const key = el.getAttribute('data-translate-placeholder');
    if (window.translations[lang] && window.translations[lang][key]) {
      el.placeholder = window.translations[lang][key];
    }
  });
  
  // Re-translate dynamic labels like lbl-location and lbl-time
  const dynamicLabels = document.querySelectorAll('.translatable-lbl');
  dynamicLabels.forEach(el => {
    const key = el.getAttribute('data-key');
    if (window.translations[lang] && window.translations[lang][key]) {
      el.innerHTML = window.translations[lang][key];
    }
  });
}

function playCuteLanguageChime() {
  try {
    const AudioContext = window.AudioContext || window.webkitAudioContext;
    if (!AudioContext) return;
    
    const audioCtx = new AudioContext();
    const now = audioCtx.currentTime;
    
    // Play a single high sweet ding for lang switch
    const osc = audioCtx.createOscillator();
    const gainNode = audioCtx.createGain();
    
    osc.type = 'sine';
    osc.frequency.setValueAtTime(880, now); // A5 note
    
    gainNode.gain.setValueAtTime(0, now);
    gainNode.gain.linearRampToValueAtTime(0.1, now + 0.01);
    gainNode.gain.exponentialRampToValueAtTime(0.001, now + 0.15);
    
    osc.connect(gainNode);
    gainNode.connect(audioCtx.destination);
    
    osc.start(now);
    osc.stop(now + 0.15);
  } catch (e) {
    console.log('Audio error:', e);
  }
}



/* ==========================================================================
   7. Stamp Tour & Match-3 Game Logic
   ========================================================================== */

function initLeaderboard() {
  const listEl = document.getElementById('leaderboard-list');
  if (!listEl) return;

  if (!db) {
    listEl.innerHTML = '<li style="padding: 10px; background: #ffe4ec; border-radius: 15px; border: 2px dashed #ffb6c1;">Leaderboard offline. Play for fun! 🐾</li>';
    return;
  }

  try {
    db.ref('leaderboard').orderByChild('score').limitToLast(10).on('value', snapshot => {
      const users = [];
      snapshot.forEach(child => {
        users.push({
          nickname: child.key,
          score: child.val().score
        });
      });
      // Sort descending
      users.sort((a, b) => b.score - a.score);

      listEl.innerHTML = '';
      if (users.length === 0) {
        listEl.innerHTML = '<li style="padding: 10px; background: #ffe4ec; border-radius: 15px; border: 2px dashed #ffb6c1;">No scores yet! Play to rank up! 🐾</li>';
        return;
      }

      users.forEach((user, index) => {
        const li = document.createElement('li');
        li.style.padding = '10px 15px';
        li.style.background = index === 0 ? '#ffb6c1' : '#ffe4ec';
        li.style.borderRadius = '15px';
        li.style.border = '2px dashed #ffb6c1';
        li.style.display = 'flex';
        li.style.justifyContent = 'space-between';
        
        const rankMedal = index === 0 ? '🥇' : index === 1 ? '🥈' : index === 2 ? '🥉' : `${index + 1}.`;
        
        li.innerHTML = `<span>${rankMedal} ${user.nickname}</span> <span>${user.score} pts</span>`;
        listEl.appendChild(li);
      });
    });
  } catch (error) {
    console.warn("Failed to load leaderboard:", error);
    listEl.innerHTML = '<li style="padding: 10px; background: #ffe4ec; border-radius: 15px; border: 2px dashed #ffb6c1;">Leaderboard offline. 🐾</li>';
  }
}

function resetGame() {
  const nicknameModal = document.getElementById('nickname-modal');
  const nicknameInput = document.getElementById('nickname-input');
  const stampTour = document.getElementById('stamp-tour');
  const interactiveMap = document.getElementById('interactive-map');
  
  playerNickname = "Guest";
  if (nicknameInput) nicknameInput.value = '';
  
  // Clean Exit Logic: Hide container and modal, show map
  if (stampTour) stampTour.style.display = 'none';
  if (interactiveMap) interactiveMap.style.display = 'block';
  if (nicknameModal) nicknameModal.style.display = 'none';
  
  // Expose global variables to reset game state. 
  // Wait, I need to reset them where they are accessible. They are in the global scope? No, they are inside initStampTour scope.
  // Actually, I can just call a global reset function.
  if (window.doResetGame) {
    window.doResetGame();
  }
}

function initStampTour() {
  const gameBoard = document.getElementById('match3-board');
  const scoreCounter = document.getElementById('score-counter');
  const scoreLegend = document.getElementById('score-legend');
  const treasureChest = document.getElementById('treasure-chest');
  const gameHeader = document.getElementById('game-header');
  const levelTitle = document.getElementById('level-title');

  if (!gameBoard) return;

  // Level System State
  let currentLevel = 1;
  const levelConfigs = {
    1: { items: ['🐯', '🩷', '🎀', '🍲', '🥞'], points: { '🩷': 10, '🎀': 20, '🍲': 30, '🥞': 40, '🐯': 50 }, target: 500 },
    2: { items: ['🍲', '🥞', '🥘', '🥟', '🍜'], points: { '🍲': 20, '🥞': 30, '🥘': 40, '🥟': 50, '🍜': 60 }, target: 1000 },
    3: { items: ['🌸', '🎵', '💖', '🎤', '🌟'], points: { '🌸': 30, '🎵': 40, '💖': 50, '🎤': 60, '🌟': 70 }, target: 2000 }
  };

  const width = 6;
  let grid = [];
  let totalScore = 0;
  
  let draggedElement = null;
  let replacedElement = null;
  let isProcessing = false;

  window.doResetGame = function() {
    currentLevel = 1;
    totalScore = 0;
    isProcessing = false;
    
    if (levelTitle) levelTitle.innerHTML = `Level ${currentLevel} 💖`;
    
    if (treasureChest) treasureChest.style.display = 'none';
    if (gameHeader) gameHeader.style.display = 'block';
    if (gameBoard) gameBoard.style.display = 'grid';
    
    updateLegend();
    createBoard();
  };

  // Event Delegation for the entire Game Board
  gameBoard.addEventListener('mousedown', dragStart);
  gameBoard.addEventListener('touchstart', touchStart, {passive: false});
  gameBoard.addEventListener('touchmove', touchMove, {passive: false});
  gameBoard.addEventListener('touchend', touchEnd, {passive: false});

  function getConfig() {
    return levelConfigs[currentLevel] || levelConfigs[3];
  }

  function getLevelItems() {
    return getConfig().items;
  }

  function updateLegend() {
    if (!scoreLegend) return;
    const config = getConfig();
    scoreLegend.innerHTML = '';
    
    config.items.forEach(item => {
      const span = document.createElement('span');
      span.className = 'legend-item';
      span.style.background = '#fff';
      span.style.padding = '5px 12px';
      span.style.borderRadius = '20px';
      span.style.border = '2px solid #ffb6c1';
      span.style.fontWeight = 'bold';
      span.style.color = '#555';
      span.style.display = 'flex';
      span.style.alignItems = 'center';
      span.style.gap = '5px';
      span.innerHTML = `${item} <span>${config.points[item]}</span>`;
      scoreLegend.appendChild(span);
    });
    
    if (scoreCounter) {
      scoreCounter.innerHTML = `Score: ${totalScore} / ${config.target}`;
    }
  }

  function createBoard() {
    gameBoard.innerHTML = '';
    grid = [];
    const items = getLevelItems();

    for (let i = 0; i < width * width; i++) {
      const cell = document.createElement('div');
      cell.classList.add('match3-cell');
      cell.setAttribute('data-id', i);
      
      let randomItem = items[Math.floor(Math.random() * items.length)];
      cell.innerHTML = randomItem;
      
      gameBoard.appendChild(cell);
      grid.push(cell);
    }
    
    // Ensure no initial matches
    let hasMatches = checkMatches(false);
    while(hasMatches) {
      grid.forEach(cell => {
         cell.innerHTML = items[Math.floor(Math.random() * items.length)];
      });
      hasMatches = checkMatches(false);
    }
  }

  // Touch support
  let touchStartX = 0;
  let touchStartY = 0;

  function touchStart(e) {
    if (isProcessing) return;
    const targetCell = e.target.closest('.match3-cell');
    if (!targetCell) return;
    
    e.preventDefault(); 
    touchStartX = e.touches[0].clientX;
    touchStartY = e.touches[0].clientY;
    draggedElement = targetCell;
  }

  function touchMove(e) {
    e.preventDefault(); // Prevent scrolling while swiping on the board
  }

  function touchEnd(e) {
    if (!draggedElement) {
      touchStartX = 0;
      touchStartY = 0;
      return;
    }
    
    const touchEndX = e.changedTouches[0].clientX;
    const touchEndY = e.changedTouches[0].clientY;
    
    const dx = touchEndX - touchStartX;
    const dy = touchEndY - touchStartY;
    
    let targetId = parseInt(draggedElement.getAttribute('data-id'));
    let isValidSwipe = false;
    
    if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 15) {
      if (dx > 0) targetId += 1; // Swipe Right
      else targetId -= 1; // Swipe Left
      isValidSwipe = true;
    } else if (Math.abs(dy) > 15) {
      if (dy > 0) targetId += width; // Swipe Down
      else targetId -= width; // Swipe Up
      isValidSwipe = true;
    }
    
    if (!isProcessing && isValidSwipe && targetId >= 0 && targetId < width * width) {
      replacedElement = grid[targetId];
      handleSwap();
    }
    
    // Hard reset
    touchStartX = 0;
    touchStartY = 0;
    draggedElement = null;
  }

  // Mouse Drag
  function dragStart(e) {
    if (isProcessing) return;
    const targetCell = e.target.closest('.match3-cell');
    if (!targetCell) return;
    
    draggedElement = targetCell;
    document.addEventListener('mouseup', dragEndMouse, {once: true});
  }

  function dragEndMouse(e) {
     if (isProcessing || !draggedElement) return;
     
     draggedElement.style.visibility = 'hidden';
     let elemBelow = document.elementFromPoint(e.clientX, e.clientY);
     draggedElement.style.visibility = 'visible';
     
     if (elemBelow && elemBelow.classList.contains('match3-cell')) {
         replacedElement = elemBelow;
         handleSwap();
     }
     draggedElement = null;
  }

  function handleSwap() {
    if (!draggedElement || !replacedElement) return;
    const draggedId = parseInt(draggedElement.getAttribute('data-id'));
    const replacedId = parseInt(replacedElement.getAttribute('data-id'));
    
    const validMoves = [ draggedId - 1, draggedId + 1, draggedId - width, draggedId + width ];
    
    if (draggedId % width === 0 && replacedId === draggedId - 1) return;
    if ((draggedId + 1) % width === 0 && replacedId === draggedId + 1) return;

    if (validMoves.includes(replacedId)) {
      let temp = draggedElement.innerHTML;
      draggedElement.innerHTML = replacedElement.innerHTML;
      replacedElement.innerHTML = temp;

      isProcessing = true;
      
      setTimeout(() => {
        let hasMatch = checkMatches(true);
        if (!hasMatch) {
          let temp = draggedElement.innerHTML;
          draggedElement.innerHTML = replacedElement.innerHTML;
          replacedElement.innerHTML = temp;
          
          // Safety unlock
          setTimeout(() => { isProcessing = false; }, 50);
        }
      }, 300);
    }
    draggedElement = null;
    replacedElement = null;
  }

  function checkMatches(executePop = true) {
    let matchedIndices = new Set();

    for (let i = 0; i < width * width; i++) {
      if (i % width > width - 3) continue;
      let rowMatch = [i, i+1, i+2];
      let val = grid[i].innerHTML;
      if (val === '') continue;
      if (grid[i+1].innerHTML === val && grid[i+2].innerHTML === val) {
        rowMatch.forEach(idx => matchedIndices.add(idx));
        if (i % width <= width - 4 && grid[i+3].innerHTML === val) matchedIndices.add(i+3);
        if (i % width <= width - 5 && grid[i+3].innerHTML === val && grid[i+4].innerHTML === val) matchedIndices.add(i+4);
      }
    }

    for (let i = 0; i < width * width - (width * 2); i++) {
      let colMatch = [i, i+width, i+(width*2)];
      let val = grid[i].innerHTML;
      if (val === '') continue;
      if (grid[i+width].innerHTML === val && grid[i+(width*2)].innerHTML === val) {
        colMatch.forEach(idx => matchedIndices.add(idx));
      }
    }

    if (matchedIndices.size > 0) {
      if (executePop) {
        let matchedValues = Array.from(matchedIndices).map(idx => grid[idx].innerHTML);
        calculateScore(matchedValues);
        popMatches(matchedIndices);
      }
      return true;
    }
    return false;
  }

  function calculateScore(matchedValues) {
    let earned = 0;
    const config = getConfig();
    matchedValues.forEach(val => {
      earned += config.points[val] || 10;
    });
    
    totalScore += earned;
    if (scoreCounter) {
      scoreCounter.innerHTML = `Score: ${totalScore} / ${config.target}`;
    }
    
    // Push Score to Firebase
    try {
      if (db && playerNickname !== "Guest") {
        db.ref('leaderboard/' + playerNickname).set({
          score: totalScore,
          timestamp: firebase.database.ServerValue.TIMESTAMP
        }).catch(err => console.warn("Firebase set error:", err));
      }
    } catch (err) {
      console.warn("Firebase score push failed:", err);
    }
    
    if (totalScore >= config.target) {
      triggerWin();
    }
  }

  function popMatches(indices) {
    indices.forEach(idx => {
      grid[idx].classList.add('pop-anim');
      setTimeout(() => {
        grid[idx].innerHTML = '';
        grid[idx].classList.remove('pop-anim');
      }, 400);
    });
    
    setTimeout(applyGravity, 400);
  }

  function applyGravity() {
    const items = getLevelItems();
    for (let i = width * width - 1; i >= width; i--) {
      if (grid[i].innerHTML === '') {
        let above = i - width;
        while (above >= 0 && grid[above].innerHTML === '') {
          above -= width;
        }
        if (above >= 0) {
          grid[i].innerHTML = grid[above].innerHTML;
          grid[i].classList.add('fall-anim');
          grid[above].innerHTML = '';
          setTimeout(() => grid[i].classList.remove('fall-anim'), 300);
        }
      }
    }
    
    for (let i = 0; i < width * width; i++) {
      if (grid[i].innerHTML === '') {
        grid[i].innerHTML = items[Math.floor(Math.random() * items.length)];
        grid[i].classList.add('fall-anim');
        setTimeout(() => grid[i].classList.remove('fall-anim'), 300);
      }
    }

    setTimeout(() => {
      let stillHasMatches = checkMatches(true);
      if (!stillHasMatches) {
        // Force unlock with safety fallback
        setTimeout(() => { isProcessing = false; }, 50);
      }
    }, 450); // Increased timeout slightly to ensure DOM has updated
  }

  function triggerWin() {
    setTimeout(() => {
      gameBoard.style.display = 'none';
      if (gameHeader) gameHeader.style.display = 'none';
      treasureChest.style.display = 'flex';
      
      treasureChest.addEventListener('click', handleNextLevel, {once: true});
    }, 1000);
  }

  function handleNextLevel() {
    // Confetti
    if (window.confetti) {
      const duration = 2000;
      const animationEnd = Date.now() + duration;
      const defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 10001, colors: ['#FF1493', '#FF69B4', '#FFB6C1', '#FFF'] };
      const interval = setInterval(function() {
        const timeLeft = animationEnd - Date.now();
        if (timeLeft <= 0) return clearInterval(interval);
        const particleCount = 50 * (timeLeft / duration);
        window.confetti(Object.assign({}, defaults, { particleCount, origin: { x: Math.random() * 0.5 + 0.25, y: Math.random() - 0.2 } }));
      }, 250);
    }

    // Advance Level
    currentLevel++;
    if(levelTitle) {
      levelTitle.innerHTML = `Level ${currentLevel} 💖`;
    }

    // Do NOT reset totalScore to 0. It accumulates!
    updateLegend();

    // Rebuild Board
    setTimeout(() => {
      treasureChest.style.display = 'none';
      if (gameHeader) gameHeader.style.display = 'block';
      gameBoard.style.display = 'grid';
      createBoard();
      isProcessing = false;
    }, 1500);
  }

  // Start game immediately
  updateLegend();
  createBoard();
}

/* ==========================================================================
   8. Video Carousel
   ========================================================================== */
function initVideoCarousel() {
  const videoPlayer = document.getElementById('main-hero-video');
  const captionEl = document.getElementById('main-hero-caption');
  const nextButton = document.getElementById('video-carousel-next');
  const prevButton = document.getElementById('video-carousel-prev');
  
  if (!videoPlayer || !captionEl || !nextButton || !prevButton) return;

  const videos = [
    {
      src: 'assets/마사야 첫눈.mp4',
      caption: '<p><span class="label" style="font-weight: 700; color: #FF1493;">❄️ First Snow (English Subtitles) - Edited by Pinktiger USA</span></p>'
    },
    {
      src: 'assets/당신과의 키스를 세어보아요.mp4',
      caption: '<p><span class="label" style="font-weight: 700; color: #FF1493;">❄️ Remembering them one by one</span></p>'
    },
    {
      src: 'assets/마지막 비 420.mp4',
      caption: '<p><span class="label" style="font-weight: 700; color: #FF1493;">❄️ Last Rain</span></p>'
    }
  ];

  let currentIndex = 0;

  function updateVideo(index) {
    // Smooth transition: fade out, update, fade in
    videoPlayer.style.opacity = '0';
    captionEl.style.opacity = '0';
    
    setTimeout(() => {
      videoPlayer.src = videos[index].src;
      captionEl.innerHTML = videos[index].caption;
      videoPlayer.load();
      videoPlayer.play().catch(e => console.log('Autoplay prevented', e));
      
      videoPlayer.style.opacity = '1';
      captionEl.style.opacity = '1';
    }, 300);
  }

  nextButton.addEventListener('click', () => {
    currentIndex = (currentIndex + 1) % videos.length;
    updateVideo(currentIndex);
  });

  prevButton.addEventListener('click', () => {
    currentIndex = (currentIndex - 1 + videos.length) % videos.length;
    updateVideo(currentIndex);
  });
  
  // Add smooth transitions for the fade effect
  videoPlayer.style.transition = 'opacity 0.3s ease';
  captionEl.style.transition = 'opacity 0.3s ease';
}

/* ==========================================================================
   8. Mobile Menu Toggle
   ========================================================================== */
function initMobileMenu() {
  const hamburgerMenu = document.querySelector('.hamburger-menu');
  const navMenu = document.querySelector('.nav-menu');

  if (hamburgerMenu && navMenu) {
    hamburgerMenu.addEventListener('click', () => {
      navMenu.classList.toggle('active');
    });

    // Close menu when a link is clicked
    const navLinks = navMenu.querySelectorAll('.nav-link');
    navLinks.forEach(link => {
      link.addEventListener('click', () => {
        navMenu.classList.remove('active');
      });
    });
  }
}

/* ==========================================================================
   9. Interactive Map Logic (Leaflet)
   ========================================================================== */
function initInteractiveMap() {
  const mapContainer = document.getElementById('map');
  if (!mapContainer || typeof L === 'undefined') return;
  
  const map = L.map('map', { 
    worldCopyJump: true,
    maxBounds: [[-90, -180], [90, 180]],
    minZoom: 2,
    maxBoundsViscosity: 1.0
  }).setView([20, 0], 2);
  
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; OpenStreetMap contributors',
    noWrap: true
  }).addTo(map);

  const pawIcon = L.divIcon({
    html: `
      <div style="color: #FF69B4; font-size: 20px; filter: drop-shadow(0px 4px 6px rgba(255, 105, 180, 0.8)); display: flex; justify-content: center; align-items: center; width: 100%; height: 100%; transform: translate(-10px, -20px); animation: pulse-paw 2s infinite;">
        <i class="fa-solid fa-paw"></i>
      </div>
      <style>
        @keyframes pulse-paw {
          0% { transform: translate(-10px, -20px) scale(1); }
          50% { transform: translate(-10px, -20px) scale(1.15); }
          100% { transform: translate(-10px, -20px) scale(1); }
        }
      </style>
    `,
    className: 'custom-paw-icon',
    iconSize: [20, 20],
    iconAnchor: [10, 20],
    popupAnchor: [0, -20]
  });

  const clusterPawIcon = L.divIcon({
    html: `
      <div style="color: #FF69B4; font-size: 26px; filter: drop-shadow(0px 0px 10px rgba(255, 105, 180, 1)); display: flex; justify-content: center; align-items: center; width: 100%; height: 100%; transform: translate(-13px, -26px); animation: pulse-cluster 1.5s infinite;">
        <i class="fa-solid fa-paw"></i>
        <div style="position: absolute; width: 40px; height: 40px; border: 2px solid rgba(255, 105, 180, 0.8); border-radius: 50%; box-shadow: 0 0 15px rgba(255, 105, 180, 0.6);"></div>
      </div>
      <style>
        @keyframes pulse-cluster {
          0% { transform: translate(-13px, -26px) scale(1); }
          50% { transform: translate(-13px, -26px) scale(1.1); }
          100% { transform: translate(-13px, -26px) scale(1); }
        }
      </style>
    `,
    className: 'custom-cluster-icon',
    iconSize: [26, 26],
    iconAnchor: [13, 26]
  });

  const markerCluster = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: false,
    zoomToBoundsOnClick: true,
    maxClusterRadius: 50,
    spiderLegPolylineOptions: { weight: 3, color: 'rgba(255, 105, 180, 0.7)', opacity: 0.8 },
    iconCreateFunction: function(cluster) {
      return clusterPawIcon;
    }
  });
  map.addLayer(markerCluster);

  const hardcodedMarkers = [
    { lat: 34.0522, lng: -118.2437, nickname: "PinkTiger_LA", message: "Been listening to his covers on repeat! Can't wait for a US tour! 🐯💖" },
    { lat: 40.7128, lng: -74.0060, nickname: "Melody_NY", message: "His vocal range is absolutely insane. This new fan site is gorgeous! 🎤✨" },
    { lat: 48.8566, lng: 2.3522, nickname: "Paris_PinkTiger", message: "Growing fanbase in Paris! Your vocal tone is incredibly trendy in Europe. 🇫🇷✨" },
    { lat: 40.4168, lng: -3.7038, nickname: "Madrid_MasayaFan", message: "Strong support from Spain! Latin fans are streaming your songs every day. 🇪🇸🔥" },
    { lat: 39.9042, lng: 116.4074, nickname: "Beijing_Tiger", message: "Huge potential for the global Asian market! Cheering from China! 🇨🇳🐯" },
    { lat: -23.5505, lng: -46.6333, nickname: "BR_PinkTigers", message: "South American fans are here! We need a global tour soon! 🇧🇷💖" },
    { lat: 51.5074, lng: -0.1278, nickname: "London_Pop", message: "Amazing talent. UK fans are definitely tuned in! 🇬🇧🎵" },
    { lat: 35.6762, lng: 139.6503, nickname: "Tokyo_PinkTiger", message: "Always waiting for your next Tokyo live! The fan energy here is amazing! 🇯🇵🐯" },
    { lat: 37.5665, lng: 126.9780, nickname: "Seoul_MasayaFan", message: "K-fans are completely mesmerized by your vocal color! Come to Korea soon! 🇰🇷✨" },
    { lat: 47.6062, lng: -122.3321, nickname: "Seattle_PinkTiger", message: "Streaming your songs every day from the rainy city! We love you Masaya! ☕🐯" },
    { lat: 33.9367, lng: -118.0278, nickname: "SoCal_Roar", message: "Huge fan from Southern California! Your high notes are amazing! 🌴✨" }
  ];

  hardcodedMarkers.forEach(item => {
    const marker = L.marker([item.lat, item.lng], { icon: pawIcon });
    
    let popupContent = `<div style="text-align:center; max-width: 250px;">`;
    popupContent += `<h4 style="color: var(--brand-pink); margin-bottom: 10px; font-size: 1.2rem; font-weight: bold;">${item.nickname}</h4>`;
    popupContent += `<p style="font-family:'Quicksand', sans-serif; color:#333; margin:0;">"${item.message}"</p>`;
    popupContent += `</div>`;
    
    marker.bindPopup(popupContent);
    markerCluster.addLayer(marker);
  });
}




function initInteractiveMap() {
  const mapContainer = document.getElementById('map');
  if (!mapContainer || typeof L === 'undefined') return;
  
  const map = L.map('map', { 
    worldCopyJump: true,
    maxBounds: [[-90, -180], [90, 180]],
    minZoom: 2,
    maxBoundsViscosity: 1.0
  }).setView([20, 0], 2);
  
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; OpenStreetMap contributors',
    noWrap: true
  }).addTo(map);

  const pawIcon = L.divIcon({
    html: `
      <div style="color: #FF69B4; font-size: 20px; filter: drop-shadow(0px 4px 6px rgba(255, 105, 180, 0.8)); display: flex; justify-content: center; align-items: center; width: 100%; height: 100%; transform: translate(-10px, -20px); animation: pulse-paw 2s infinite;">
        <i class="fa-solid fa-paw"></i>
      </div>
      <style>
        @keyframes pulse-paw {
          0% { transform: translate(-10px, -20px) scale(1); }
          50% { transform: translate(-10px, -20px) scale(1.15); }
          100% { transform: translate(-10px, -20px) scale(1); }
        }
      </style>
    `,
    className: 'custom-paw-icon',
    iconSize: [20, 20],
    iconAnchor: [10, 20],
    popupAnchor: [0, -20]
  });

  const clusterPawIcon = L.divIcon({
    html: `
      <div style="color: #FF69B4; font-size: 26px; filter: drop-shadow(0px 0px 10px rgba(255, 105, 180, 1)); display: flex; justify-content: center; align-items: center; width: 100%; height: 100%; transform: translate(-13px, -26px); animation: pulse-cluster 1.5s infinite;">
        <i class="fa-solid fa-paw"></i>
        <div style="position: absolute; width: 40px; height: 40px; border: 2px solid rgba(255, 105, 180, 0.8); border-radius: 50%; box-shadow: 0 0 15px rgba(255, 105, 180, 0.6);"></div>
      </div>
      <style>
        @keyframes pulse-cluster {
          0% { transform: translate(-13px, -26px) scale(1); }
          50% { transform: translate(-13px, -26px) scale(1.1); }
          100% { transform: translate(-13px, -26px) scale(1); }
        }
      </style>
    `,
    className: 'custom-cluster-icon',
    iconSize: [26, 26],
    iconAnchor: [13, 26]
  });

  const markerCluster = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: false,
    zoomToBoundsOnClick: true,
    maxClusterRadius: 50,
    spiderLegPolylineOptions: { weight: 3, color: 'rgba(255, 105, 180, 0.7)', opacity: 0.8 },
    iconCreateFunction: function(cluster) {
      return clusterPawIcon;
    }
  });
  map.addLayer(markerCluster);

  const hardcodedMarkers = [
    { lat: 34.0522, lng: -118.2437, nickname: "PinkTiger_LA", message: "Been listening to his covers on repeat! Can't wait for a US tour! 💖" },
    { lat: 40.7128, lng: -74.0060, nickname: "Melody_NY", message: "His vocal range is absolutely insane. This new fan site is gorgeous! ✨" },
    { lat: 48.8566, lng: 2.3522, nickname: "Paris_PinkTiger", message: "Growing fanbase in Paris! Your vocal tone is incredibly trendy in Europe. 🗼" },
    { lat: 40.4168, lng: -3.7038, nickname: "Madrid_MasayaFan", message: "Strong support from Spain! Latin fans are streaming your songs every day. 🎶" },
    { lat: 39.9042, lng: 116.4074, nickname: "Beijing_Tiger", message: "Huge potential for the global Asian market! Cheering from China! 🐅" },
    { lat: -23.5505, lng: -46.6333, nickname: "BR_PinkTigers", message: "South American fans are here! We need a global tour soon! 🌎" },
    { lat: 51.5074, lng: -0.1278, nickname: "London_Pop", message: "Amazing talent. UK fans are definitely tuned in! 🎵" },
    { lat: 35.6762, lng: 139.6503, nickname: "Tokyo_PinkTiger", message: "Always waiting for your next Tokyo live! The fan energy here is amazing! 🗼" },
    { lat: 37.5665, lng: 126.9780, nickname: "Seoul_MasayaFan", message: "K-fans are completely mesmerized by your vocal color! Come to Korea soon! 🇰🇷" },
    { lat: 47.6062, lng: -122.3321, nickname: "Seattle_PinkTiger", message: "Streaming your songs every day from the rainy city! We love you Masaya! ☔" },
    { lat: 33.9367, lng: -118.0278, nickname: "SoCal_Roar", message: "Huge fan from Southern California! Your high notes are amazing! 🌴" }
  ];

  hardcodedMarkers.forEach(item => {
    const marker = L.marker([item.lat, item.lng], { icon: pawIcon });
    
    let popupContent = `<div style="text-align:center; max-width: 250px;">`;
    popupContent += `<h4 style="color: var(--brand-pink); margin-bottom: 10px; font-size: 1.2rem; font-weight: bold;">${item.nickname}</h4>`;
    popupContent += `<p style="font-family:'Quicksand', sans-serif; color:#333; margin:0;">"${item.message}"</p>`;
    popupContent += `</div>`;
    
    marker.bindPopup(popupContent);
    markerCluster.addLayer(marker);
  });
}

/* ==========================================================================
   10. Mascot Easter Egg
   ========================================================================== */
function initMascotEasterEgg() {
  const mascotTrigger = document.getElementById('mascot-trigger');
  const eeModal = document.getElementById('easter-egg-modal');
  const eeImage = document.getElementById('ee-image');
  const eeModalContent = document.getElementById('ee-modal-content');
  
  if (!mascotTrigger || !eeModal || !eeImage) return;

  let eeCount = 0;
  const eeTotal = 7;
  let eeTimeout = null;

  // Preload all images for instant display
  const imagePaths = [];
  for (let i = 1; i <= eeTotal; i++) {
    const path = `assets/마사야 사진 ${i}.png`;
    imagePaths.push(path);
    const preloadImg = new Image();
    preloadImg.src = path;
  }

  const hideModal = () => {
    eeModal.style.opacity = '0';
    eeModal.style.pointerEvents = 'none';
    eeModal.style.visibility = 'hidden';
    if (eeTimeout) {
      clearTimeout(eeTimeout);
      eeTimeout = null;
    }
  };

  mascotTrigger.addEventListener('click', (e) => {
    e.stopPropagation(); // Prevent sparkle click handler from interfering
    eeCount = (eeCount % eeTotal) + 1; // cycles 1 to 7
    
    // Set image source (no encodeURI - browser handles Korean filenames natively)
    eeImage.src = imagePaths[eeCount - 1];
    
    // Ensure image element is visible and on top
    eeImage.style.display = 'block';
    eeImage.style.visibility = 'visible';
    eeImage.style.opacity = '1';
    eeImage.style.zIndex = '2';
    
    // Show modal with fade
    eeModal.style.opacity = '1';
    eeModal.style.pointerEvents = 'auto';
    eeModal.style.visibility = 'visible';
    
    // Reset animation
    if (eeModalContent) {
      eeModalContent.classList.remove('heart-pop-anim');
      void eeModalContent.offsetWidth; // Trigger reflow
      eeModalContent.classList.add('heart-pop-anim');
    }

    // Auto-hide after 3 seconds
    if (eeTimeout) clearTimeout(eeTimeout);
    eeTimeout = setTimeout(hideModal, 3000);
  });

  // Close modal when clicking anywhere on the modal
  eeModal.addEventListener('click', hideModal);
}

/* ==========================================================================
   11. Fan Letter Carousel
   ========================================================================== */

window.scrollFanLetters = function(amount) {
  const container = document.getElementById('home-letters-container');
  if (container) {
    // On mobile, scroll by the full width of the container instead of fixed pixel amount
    let scrollAmount = amount;
    if (window.innerWidth <= 768) {
      scrollAmount = amount > 0 ? container.clientWidth : -container.clientWidth;
    }
    
    container.scrollTo({
      left: container.scrollLeft + scrollAmount,
      behavior: 'smooth'
    });
    setTimeout(() => {
      if (window.updateFanLetterButtons) window.updateFanLetterButtons();
    }, 400); // Wait for smooth scroll to finish
  }
};

function initFanLetterCarousel() {
  const container = document.getElementById('home-letters-container');
  const prevBtn = document.getElementById('fanletter-prev');
  const nextBtn = document.getElementById('fanletter-next');
  
  if (!container || !prevBtn || !nextBtn) return;

  const updateButtons = () => {
    // Disable prev if at start
    prevBtn.disabled = container.scrollLeft <= 0;
    // Disable next if at end (allow 1px rounding error)
    nextBtn.disabled = container.scrollLeft + container.clientWidth >= container.scrollWidth - 1;
  };

  container.addEventListener('scroll', updateButtons);
  window.addEventListener('resize', updateButtons);
  
  // Initial check (delay slightly to allow Firebase injection)
  setTimeout(updateButtons, 1000);
  
  // Expose update function to be called after Firebase injection
  window.updateFanLetterButtons = updateButtons;
}

document.addEventListener('DOMContentLoaded', () => {
  initMascotEasterEgg();
  initFanLetterCarousel();
});

/* ==========================================================================
   Goods Store Swiper Initialization
   ========================================================================== */
function initGoodsSwiper() {
  if (typeof Swiper !== 'undefined') {
    new Swiper('.goodsSwiper', {
      slidesPerView: 1,
      spaceBetween: 20,
      loop: true,
      pagination: {
        el: '.swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
      },
      breakpoints: {
        768: {
          slidesPerView: 2,
          spaceBetween: 30,
        },
        1024: {
          slidesPerView: 3,
          spaceBetween: 30,
        }
      }
    });
  }
}
